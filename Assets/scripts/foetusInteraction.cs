using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class foetusInteraction : MonoBehaviour
{
    //Script d'interaction avec le foetus -- Il est possible de mettre le foetus dans la marmite et obtenir du compost!
    //Déclaration des variables
    public AudioClip foetusSonTransformation; //Son pour la transformation du foetus 

    void Update()
    {
        //private void OnCollisionEnter(Collision infoCollision)
        {
            //Lorsqu'il y a collision avec la marmite, le foetus se transforme en sac de terre après 3 secondes
            //if ((infoCollision.gameObject.CompareTag("marmite")) && (infoCollision.gameObject.name("foetus")))
            {

            }
        }
    }
}
